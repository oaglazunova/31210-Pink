// Configure grunt
  grunt.initConfig({
    sprite:{
      all: {
        src: 'img/sprites-source/*.png',
        dest: 'img/spritesheet.png',
        destCss: 'img/sprites-source/sprites.css'
      }
    }
  });

  // Load in `grunt-spritesmith`
  grunt.loadNpmTasks('grunt-spritesmith');
  
  //Run the 'grunt sprite' task